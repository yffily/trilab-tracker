name: trilab-tracker
channels:
  - defaults
dependencies:
  - python=3.7
  - pip
  - jupyter
  - numpy
  - scipy
  - pandas
  - matplotlib
  - pyqtgraph
  - xlrd # read excel spreadsheets
  - openpyxl # write excel spreadsheets
  - opencv
  - pip:
#    - opencv-python
#    - opencv-contrib-python # Install instead of opencv-python. Contains extra features including extra background subtraction methods in submodule cv2.bgsegm. 
    - sklearn
    - argparse
    - screeninfo # get screen resolution
    - PythonVideoConverter # ffmpeg based video conversion
    - flatten-dict
